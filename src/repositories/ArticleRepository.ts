import {ArticleRepositoryI} from "../models/ArticleModel";

export class ArticleRepository implements ArticleRepositoryI {
    findArticle(article_id: number) {
        return null;
    }
    findUserArticles(user_id: number) {
        return null;
    }
}